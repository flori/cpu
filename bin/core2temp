#!/usr/bin/env ruby

require 'msr'

if temperature_junction = ARGV.shift
  MSR::CPU.temperature_junction = temperature_junction.to_i
end

MSR.each do |cpu|
  puts "#{cpu.number}\t#{cpu.temperature}"
end
